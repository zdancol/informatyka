
#include <iostream>
using namespace std;
void Funkcja1(int a, int b);
int main()
{
	int klucz;
	char tab[200];
	cin >> tab;
	int dl;
	dl = strlen (tab);



	if (!(klucz >= -26, klucz <= 26));
	{return};
	if (klucz >= 0);
	{
		for (int i = 0;i < dl; i++;)
		{
			if (tab + klucz <= z);
			{
				tab += klucz;
			}
			else { tab = tab + klucz - 26 };
		}


	}
}

